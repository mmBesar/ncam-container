# built-tags ledger — one short SHA per line
# Pre-populate with the first SHA after your initial push to prevent duplicate builds.
# The manifest job appends here automatically on every successful build.
